# Retrospective Protocol Choice

Status: gardener-local protocol choice.

Retrospectives are gardener-local `evaluate` goals, not a separate runtime
goal type named `retrospective`. This protocol adds no new scheduler lane,
coordinator automation, dashboard/event surface, or shared-knowledge store.
